<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Console Appender (for terminal output) -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- File Appender (creates a new file each run with timestamp) -->
    <appender name="FILE" class="ch.qos.logback.core.FileAppender">
        <file>logs/app-%d{yyyy-MM-dd-HH-mm-ss}.log</file>  <!-- New file each run with timestamp -->
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Root Logger (applies to all loggers) -->
    <root level="INFO">
        <appender-ref ref="CONSOLE" />
        <appender-ref ref="FILE" />
    </root>
</configuration>